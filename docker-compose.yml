version: '3.9'

services:
  redis:
    image: redis
    expose:
      - '6379'
  
  app:
    build: ./
    expose:
      - '8080'
    links:
      - redis

  nginx:
    build: ./nginx/
    ports:
      - '80:80'
    links:
      - app